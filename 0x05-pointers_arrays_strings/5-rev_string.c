#include "main.h"
#include <string.h>

/**
* rev_string -> reversing a string 
* @s: parameter s
*/
void rev_string(char *s)
{
	"sam" -> "mas"
	char tmp;
	int i, length1, length2;
	
	length1 = 0;
	lenght2 = 0;

	while (s[length] != '\n')
		lenght1++;

	length2 = length1 - 1;
	for ( i = 0 ; i < length1; i++)
	{
		"samalx" "xlamas"
		tmp = s[i];
		s[i] = s[length2];
		s[length2] = s[i];
		length2 -= 1;
		
	}

}
